# Copy to .env.docker and set APP_KEY before `docker compose`:
#   cp .env.docker.example .env.docker
#   php artisan key:generate --show   # paste the value into APP_KEY
APP_KEY=
DB_DATABASE=fone_ninja
DB_USERNAME=fone_ninja
DB_PASSWORD=secret
FRONTEND_PORT=8080
